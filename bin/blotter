#!/usr/bin/env ruby

require 'blotter'
require 'logger'
require 'trollop'

opts = Trollop

file = (ARGV.first && File.open(ARGV.first)) || STDIN
parser = Blotter::Parser.new(file)
result = parser.parse
